# features/

Business features of the application (auth, users, products, etc).

Each feature is self-contained:
- UI components
- API calls to backend
- Hooks
- Pages

If code belongs to one domain only, it belongs here.
