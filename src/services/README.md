# services/

Global API and HTTP configuration.

This is where:
- API base URL is configured
- Interceptors live (auth, error handling)

Feature-specific API calls must live in:
features/<feature>/services/
